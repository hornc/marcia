from setuptools import setup, find_packages

setup(
    name="MARC-IA",
    version="0.0.1",
    description="Convert MARC XML to Internet Archive online resource MARC.",
    author="Charles Horn",
    packages=find_packages(),
)
